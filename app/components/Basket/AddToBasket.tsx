'use client';
import React from 'react'

const AddToBasket = () => {
  return (
    <div>
        <button className='btn btn-active' onClick={() => console.log('Click')}> Add to Basket</button>
    </div>
  )
}

export default AddToBasket